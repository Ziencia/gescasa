{% extends '::base.html.twig' %}

{% block body -%}
    <h1>info creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('info') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
