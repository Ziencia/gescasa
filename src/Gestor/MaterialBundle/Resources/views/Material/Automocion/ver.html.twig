{% extends 'ExpedientesBundle:Expediente:expedientes.html.twig' %}

{% block id 'material_automocion_ver' %}
{% block title %}Datos del material automocion:{{material.id}}, del expediente {{expediente.referencia}} {% endblock %}

{% block contenido %}
<h2>{{ block('title') }}</h2>
<div class="limpiar"></div>

<table class="tabla_datos_detalle">
    <tbody>
        <tr>
            <th>Tipo:</th>
            <td>{{ material.tipo|default('') }}</td>
        </tr>
        <tr>
            <th>Marca:</th>
            <td>{{ material.marca|default('') }}</td>
        </tr>
        <tr>
            <th>Modelo:</th>
            <td>{{ material.modelo|default('') }}</td>
        </tr>
        <tr>
            <th>N. Serie:</th>
            <td>{{ material.numserie|default('') }}</td>
        </tr>
        <tr>
            <th>Descripcion:</th>
            <td>{{ material.descripcion|default('') }}</td>
        </tr>
        <tr>
            <th>Bastidor:</th>
            <td>{{ material.bastidor|default('') }}</td>
        </tr>
        <tr>
            <th>Concesionario:</th>
            <td>{{ material.concesionario|default('') }}</td>
        </tr>
        <tr>
            <th>Kit:</th>
            <td>{{ material.kit|default('') }}</td>
        </tr>
        <tr>
            <th>Motor:</th>
            <td>{{ material.motor|default('') }}</td>
        </tr>
        <tr>
            <th>Color:</th>
            <td>{{ material.color|default('') }}</td>
        </tr>
        <tr>
            <th>Matricula:</th>
            <td>{{ material.matricula|default('') }}</td>
        </tr>
        <tr>
            <th>Observaciones:</th>
            <td>{{ material.observaciones|default('') }}</td>
        </tr>
        <tr>
            <th>Destino:</th>
            <td>{{material.destino|default('')}}</td>
        </tr>
        <tr>
            <th>Cantidad:</th>
            <td>{{ material.cantidad|default('') }}</td>
        </tr>
    </tbody>
</table>

<div class="contenedor_administrador_acciones">
    <ul class="administrador_acciones">
        <li>
           <input type="button" value="Volver al material" class="boton" onclick="window.location='{{ path('material_indice', { 'id': expediente.id }) }}';"/>                   
        </li>
        <li>
           <input type="button" value="Modificar" class="boton" onclick="window.location='{{ path('material_automocion_editar', { 'id': material.id }) }}';"/>  
        </li>    
        <li>
            <form action="{{ path('material_automocion_eliminar', { 'id': material.id }) }}" method="post" class="formulario_oculto" onclick="return confirm('¿Esta seguro que quiere eliminar el material automoción:{{material.id}}, del expediente {{ expediente.referencia }} ?');">
                 {{ form_widget(delete_form_eliminar) }}
            <button type="submit" class="boton">Eliminar material</button>
            </form>
        </li>
    </ul>
  </div>

{% endblock contenido %}